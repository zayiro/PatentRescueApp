export const APP_BASE_URL = 'https://esdecali.com/truedoctor';

export const headerAxiosApp = {
    'Content-Type': 'application/json',
    'Cache-Control': 'no-cache',
    'Pragma': 'no-cache',
    'Expires': '0',
    // 'Authorization': `Bearer ${token}`, // Si usas auth
}

export const timeoutAxios = 10000; //10 segundos
